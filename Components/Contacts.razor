@using GameStore.Client.Components.Shared

@if (contactsData == null)
{
    <MudProgressLinear Color="Color.Primary" Indeterminate="true" Class="my-7" />
}
else
{
    @if (contactsData.Data.GetPublicContacts.Count == 0)
    {
        <MudAlert Severity="Severity.Info">No Public Contacts yet!</MudAlert>
    }
    else
    {
        
    }
}

<CreateContactBtn />



